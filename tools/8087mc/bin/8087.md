# Intel 8087 microcode explained

Each x87 instruction has an opcode of 11 bits. I'll call it a macro-opcode to distinguish it from the microcode opcodes.

The microcode consists of 16-bit words, and there are 1648 words in total. There are two temporary registers called tmpA and tmpB, they can hold a full 80-bit floating point value each.

I will denote individual bits with letters: `ABCDEFGHIJKLMNOP` with A being the most significant bit, and P the least significant bit.

# Opcodes

## Data movement

`ABCIJ=00000` Copies data from one register to another. Data: `DEFGH` (source register) and `KLMNO` (target register). The function of `P` is unknown but it does do something.

`ABCJ=0001` Loads a constant from the ROM. Constant index in `DEFGHI`, but it doesn't seem to be enough. How do we decide which constant to read? Random logic not in the microcode? Destination register in `KLMNO`, seemingly consisting of only the values `K.M.O` and `KLM..`.

List of known register indices:

0x02: st(0)

0x03: st(i) - i probably comes from the macro-opcode.

0x04: bus interface unit

0x06: zero - this one is also used as a target and I don't know what it means there.

0x08: exponentA

0x09: signA, exponentA

0x0a: fractionA

0x0b: tmpA (sign, exponent, fraction)

0x0c: exponentB

0x0d: signB, exponentB

0x0e: fractionB

0x0f: tmpB (sign, exponent, fraction)

0x10: "B op" output X

0x11: "B op" output Y

0x14: shift left out X

0x15: shift right out

0x17: tmpC

0x1c: shift left out Y, difference between X&Y unknown

0x1d: (not used)

## Control flow

`ABC=100` Far jump. The target is in `DEFGHI`, an index to a lookup table. Read more in "Jump targets".

`ABC=101` Far call.  The target is in `DEFGHI`, an index to a lookup table. Read more in "Jump targets".

`ABC=110` Local jump. The offset is a *signed* value in `DEFGHI`, and is added to the address of the next instruction. So, backward jumps have the `D` bit set for a negative value.

`ABCDEFGHI=111000000` Return from subroutine call.

All jumps, calls and returns use `JLKMNO` as the condition for the jump. The `P` bit inverts the condition. For example, `JKLMN..` is an unconditional jump/call. Curiously, `JKLMN.P` is used exactly once - it's the inverse of an unconditional jump/call, meaning "never take this jump/call"?

`ABCDEFGHIJKLMNO.` Everything 1 except P=0. Read next instruction - return from processing the current instruction.

`ABCDEFG.IJKLMNO.` Everything 1 except HP=0. Read next instruction, but only in the `#0675` and `#0695` functions.

### Jump conditions

0x00: is_memory_operand

0x01: (opcode&0x7C0) == 0x4C0

0x02: stack_empty

0x03: fld not fild (have to determine opcode bits)

0x04: f32 not f64 (have to determine opcode bits)

0x06: pop_at_end

0x0a: opcode&1

0x0b: double_pop_at_end

0x20: tmpA zero

0x22: tmpA bad?

0x23: stack overflow

0x24: tmpB zero

0x26: tmpB bad?

0x27: st(i) doesn't exist (A)?

0x2f: st(i) doesn't exist (B)?

0x38: accumulator!=0

0x3e: unconditional

## Bitshifts

`ABCJ=0010` performs a bitshift. `DEFGH` is the input register. 

`I=1` is a right shift `>>` and outputs to register `0x15`. 

`I=0` is a left shift `<<` and outputs to registers `0x14` and `0x1c`, the difference between them is not clear yet. 

Shift amount is in `KLMNOP`. 

If `J=1`, it's unclear if it does a shift. Maybe it loads a constant using `DEFGHI` as an index, then does the shift as usual.

## "B operation" (unknown)

`ABC=010` performs something. Seems to output to register 0x10 when `L=0`, or 0x11 when `L=1`. Seems to use `DEFGH` as an input register and `MNO` as a parameter or maybe operation code. Function of `IJK` or `P` totally unknown. More parameters?

## ABC instructions

All these instructions have `ABC=111`, `DEF` is the type and `MNO` is the parameter, other bits are always zero.

### Type 1: ???

Parameters 0 and 1.

### Type 2: ???

Parameters 0, 1, 3.

### Type 3: ???

Parameter always 0. 

### Type 4: Exceptions

`ABCD........   .` Set exception. The parameter contains the exception index from 0 to 5 in the same order as in the status word, except the bit `O` is inverted. So: 

- 0="denorm"
- 1="invalid"
- 2="overflow"
- 3="division by zero"
- 4="precision"
- 5="underflow"

### Type 5:

Parameters 4, 5, 6, 7.

## Stack manipulation

`.BCDE...........` Increment stack pointer (pop).

`.BCDE.........O.` Decrement stack pointer (push).

## Miscellaneous

`ABCDEFGHIJKLM...` No operation. NOP.

There are two that are seemingly related to each other, but I don't know what they do yet:

`.BCDEF..........` Increment something?

`.BCDEF........O.` Decrement something?

# Jump targets

Far calls and jumps have 22 targets they can go to, numbered from J0 to J21. Here's everything I know so far.

J0: Probably calls J1/J2/J3 depending on whether it's FADD/FSUB, FMUL or FDIV.

J1: FMUL ?

J2: FDIV ?

J3: FADD/FSUB ? 29 lines + calls J11

J4: Loads f32 and f64

J5: 

J6: Loads f80 and i64

J7: Loads b80

J8: Stores f32 and f64

J9: Writes out store (FST..) values to the BIU.

J10: Stores f80 and i64

J11:

J12: Checks tmpA for denorm (exp)/precision (mant) exceptions

J13: Only jumped to from FYL2X,FYL2XP1, does some final processing, then RNI.

J14: 

J15: 13 lines

J16: 

J17: 16 lines + calls J11

J18: 

J19: splits tmpA to mant/exp. return: tmpA gets mant, tmpB gets exp

J20: 12 lines

J21: 7 lines

# Problems

There are two unaccounted-for entry points `#0675` and `#0695`. It is also the only place with the `ABCDEFG.IJKLMNO.` opcode, seemingly used for RNI.
