# Intel 8087 microcode explained

The microcode consists of 16-bit words, and there are 1648 words in total. There are two temporary registers called tmpA and tmpB, they can hold a full 80-bit floating point value.

Each instruction has an opcode of 11 bits.

I will denote individual bits with letters: `ABCDEGHIJKLMNOP` with A being the most significant bit, and P the least significant bit. In mask definitions, an uppercase letter means the bit is set, and a dot `.` means the bit is unset.

# Opcodes

## Data movement

Copies data from one register to another.

`...     ..     .` The opcode looks a bit silly but it's all zeros except for DEFGH (source register) and KLMNO (target register).

List of known register indices:

0x02: st(0)

0x03: st(i) - i probably comes from the opcode.

0x06: zero - this one is also used as a target and I don't know what it means there.

0x0b: tmpA

0x0e: tmpB

## Control flow

`A..` Far jump. The target is in `DEFGHI`, an index to a lookup table. Read more in "Jump targets".

`A.C` Far call.  The target is in `DEFGHI`, an index to a lookup table. Read more in "Jump targets".

`AB.` Local jump. The offset is a *signed* value in `DEFGHI`, and is added to the address of the next instruction. So, backward jumps have the `D` bit set for a negative value.

All jumps/calls use `JLKMNO` as the condition for the jump. The `P` bit inverts the condition. `JKLMN..` is an unconditional jump/call. Curiously, `JKLMN.P` is used exactly once - it's the inverse of an unconditional jump/call, meaning "never take this jump/call"?

`ABC......JKLMN..` Return from subroutine call.

`ABCDEFGHIJKLMNO.` Read next instruction - return from processing the current instruction.

### Jump conditions

TODO

## Exceptions

`ABCD........   .` Set exception. The bits `MNO` contain the exception index from 0 to 5 in the same order as in the status word, except the bit `O` is inverted. So: 

- 0="denorm"
- 1="invalid"
- 2="overflow"
- 3="division by zero"
- 4="precision"
- 5="underflow"

Maybe `O` is flipped consistently in the microcode?

## Stack manipulation

`.BCDE...........` Increment stack pointer (pop).

`.BCDE.........O.` Decrement stack pointer (push).

There are two that are seemingly related, but I don't know what they do yet:

`.BCDEF..........` Increment something?

`.BCDEF........O.` Decrement something?

## Miscellaneous

`ABCDEFGHIJKLM...` No operation. NOP.

`...      JKLM...` Loads a constant from the ROM. Parameter in `DEFGHI`. Parameter might be the destination register - how do we decide which constant to read?

# Jump targets

Far calls and jumps have 22 targets they can go to, numbered from J0 to J21. Here's everything I know so far.

J0: Probably calls J1/J2/J3 depending on whether it's FADD/FSUB, FMUL or FDIV.

J1: FADD/FSUB ?

J2: FMUL ?

J3: FDIV ?

J4: Called when loading operands from memory

J5: 

J6: J4 branches immediately here on condition 0x07 (cf J8/J10)

J7: 

J8: Called when storing operands into memory

J9: Always jumped into, never called - assumption: a final processing step.

J10: J8 branches immediately here on condition 0x07 (cf J4/J6)

J11: 

J12: 

J13: Only jumped to from FYL2X,FYL2XP1, does some final processing, then RNI.

J14: 

J15: 

J16: 

J17: 

J18: 

J19: 

J20: 

J21: 








